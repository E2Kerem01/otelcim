# t4 bulguları

- BUG-t4-001 — Orta — `lib/features/listings/domain/listing_model.dart:120`: `images` içindeki hatalı Firestore değerleri `String` olarak zorlanıyor ve ilan parse edilirken exception oluşuyor. Repro: `malformed image data is ignored instead of crashing listing parsing`. Beklenen: hatalı elemanlar atlanmalı veya güvenli varsayılan kullanılmalı; gerçek: parse çöküyor.
- BUG-t4-002 — Düşük — `lib/shared/services/listing_service.dart:60,373`: arama `toLowerCase()` ile yapıldığı için Türkçe `İ/ı` büyük-küçük harf eşleşmesi güvenilir değil. Repro: `Turkish dotted-I search is case-insensitive for listing locations`. Beklenen: `İstanbul` için `istanbul` eşleşmeli; gerçek: eşleşmeyebilir.
- BUG-t4-003 — Düşük — `lib/shared/services/listing_service.dart:336,409`: `limit: 0` ile sorgu bir belge çekip `hasMore: true` döndürüyor. Repro: `zero page size does not advertise a phantom next page`. Beklenen: boş sayfa ve `hasMore: false`; gerçek: `hasMore: true`.
- BUG-t4-004 — Orta — `lib/features/seasonal/services/seasonal_service.dart:36-49`: sezon pencereleri 2025 yılına sabitlenmiş. Repro: `seasonal windows include the current calendar year`. Beklenen: güncel sezonların sunulması; gerçek: 2026 ve sonrası için güncel pencere yok.
- BUG-t4-005 — Kritik — `lib/features/listings/presentation/edit_listing_screen.dart:187-223`: düzenleme sırasında `isUrgent`, `lat` ve `lng` yeni `Listing` nesnesine aktarılmıyor. Repro: `saving without edits preserves urgent and geocoded fields`. Beklenen: alanların korunması; gerçek: acil işareti ve koordinatlar kayboluyor.
